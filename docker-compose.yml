version: "1.0"
services:
  worker:
    build: .
    environment:
      API_ID: $API_ID
      API_HASH: $API_HASH
      HEROKU_APP_NAME: $HEROKU_APP_NAME
      HEROKU_API: $HEROKU_API
      SESSION: $SESSION